pipeline {
  agent any

  environment {
    // --- app & docker metadata
    APP_NAME           = 'bof-careers'                  // hindari titik/space utk nama compose
    DOCKER_HUB_USER    = 'auliaanrhm'
    DOCKER_TAG         = 'latest'                       // bisa ganti ke "${BUILD_NUMBER}"
    DOCKER_IMAGE       = "${DOCKER_HUB_USER}/${APP_NAME}:${DOCKER_TAG}"
    DOCKER_CREDENTIALS = 'dockerhub-credentials'
  }

  stages {
    stage('Checkout') {
      steps {
        echo "📥 Checkout ${env.APP_NAME}…"
        git branch: 'main', url: 'https://github.com/auliaanrhm/uas_fp-main.git'
      }
    }

    stage('Build Docker Image') {
      steps {
        echo "🐳 Build image ${env.DOCKER_IMAGE}"
        bat  'docker build -t %DOCKER_IMAGE% .'
      }
    }

    stage('Push to Docker Hub') {
      steps {
        echo "📤 Push ${env.DOCKER_IMAGE}"
        withCredentials([usernamePassword(
          credentialsId: env.DOCKER_CREDENTIALS,
          usernameVariable: 'USER',
          passwordVariable: 'PASS'
        )]) {
          bat 'docker login -u %USER% -p %PASS%'
          bat 'docker push %DOCKER_IMAGE%'
        }
      }
    }

    stage('Run Docker Compose') {
      steps {
        echo '🚀 docker compose up'
        bat 'set "COMPOSE_PROJECT_NAME=%APP_NAME%" && docker compose down --remove-orphans'
        bat 'set "COMPOSE_PROJECT_NAME=%APP_NAME%" && docker compose up -d --build'
      }
    }
  }

  post {
    success { echo '✅ Pipeline finished successfully!' }
    failure { echo '❌ Pipeline failed.' }
  }
}
